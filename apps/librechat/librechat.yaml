version: 1.3.13
cache: true

mcpSettings:
  allowedDomains:
    - "mcp-server"

mcpServers:
  mcp-server:
    type: streamable-http
    url: http://mcp-server:8000/mcp
    timeout: 30000
    initTimeout: 15000
    serverInstructions: true
    chatMenu: true
    startup: true
